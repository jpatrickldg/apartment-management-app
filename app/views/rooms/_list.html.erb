<% if tenant.any? %>
  <div class="overflow-x-auto relative rounded-md">
    <table class="w-full text-sm text-left text-gray-500">
      <thead class="text-xs text-gray-800 uppercase bg-gray-200 lg:table-fixed">
        <th class="p-3">Email</th>
        <th class="p-3">First Name</th>
        <th class="p-3">Last Name</th>
        <th class="p-3 text-center">
          <span class="sr-only">Profile</span>
        </th>
      </thead>
      <tbody>
        <% tenant.each do |tenant| %>
          <tr>
            <td class="p-3 font-bold"><%= tenant.email %></td>
            <td class="p-3"><%= tenant.first_name %></td>
            <td class="p-3"><%= tenant.last_name %></td>
            <td class="p-3 text-center"><%= link_to 'Profile', tenant_path(tenant) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% else %>
  <div>
    <p class="italic text-sm">Empty</p>
  </div>
<% end %>