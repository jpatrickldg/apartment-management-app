<!DOCTYPE html>
<html>
  <head>
    <title>MWPMI Portal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="relative flex md:place-items-center justify-center h-screen w-auto shadow-md bg-gray-100 px-2">
    <div class="flex-auto">
      <div class="max-w-md p-5 rounded-md shadow-lg m-auto mt-5 bg-white">
        <h1 class="font-bold text-xl text-center mb-2">MWPMI Portal</h1>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
